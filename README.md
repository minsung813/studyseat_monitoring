# 열람실 좌석 자동 모니터링 시스템

```
# 최신화 코드
git checkout main
git pull origin main
git checkout 개인브랜치
git merge main
```

```
# git commit & push 코드
git status
git add 바뀐코드
git commit -m "메세지"
git push origin 개인브랜치
```

```
# 가상환경 활성화
conda activate 가상환경이름
```

```
# merge 방법
git checkout main
git pull origin main
git merge 개인브랜치
git push origin main
```